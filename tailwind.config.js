/** TailwindCSS v3 config for the Remotion widget bundle.
 *  Scans all renderer source so widget utility classes are generated.
 */
module.exports = {
  content: ["./src/**/*.{ts,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};