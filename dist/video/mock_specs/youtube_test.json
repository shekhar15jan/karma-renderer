{
  "title": "Optimizing with useMemo",
  "subtitle": "React Performance 101",
  "layout": "code-explainer",
  "theme": "codeatcloud",
  "components": [
    {
      "type": "heading",
      "label": "The Problem"
    },
    {
      "type": "paragraph",
      "data": {
        "body": "When a component re-renders, all variables inside it are recalculated. If you have an expensive calculation, this blocks the main thread and causes UI stuttering."
      }
    },
    {
      "type": "heading",
      "label": "The Solution"
    },
    {
      "type": "paragraph",
      "data": {
        "body": "useMemo caches the result between renders. It only recalculates if the dependencies change."
      }
    },
    {
      "type": "code",
      "data": {
        "lang": "tsx",
        "text": "import { useMemo } from 'react';\n\nconst MyComponent = ({ data }) => {\n  // This calculation is expensive\n  const processedData = useMemo(() => {\n    return data.map(item => item * 2).sort();\n  }, [data]);\n\n  return <div>{processedData.length}</div>;\n};"
      }
    }
  ]
}
